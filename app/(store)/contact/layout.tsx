import { STORE_PAGE_SEO } from "@/lib/store-pages";

export const metadata = STORE_PAGE_SEO.contact;

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
